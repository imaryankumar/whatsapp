import { View, Text } from "react-native";

const index = () => {
  return (
    <View className="flex-1 justify-center items-center">
      <Text className="font-bold text-xl bg-red-500">index</Text>
    </View>
  );
};

export default index;
